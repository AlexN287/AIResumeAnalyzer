spring:
  jpa:
    show-sql: false

  ai:
    mcp:
      client:
        enabled: false

logging:
  level:
    org.springframework.ai: INFO
    org.springframework.ai.mcp: INFO
    org.springframework.ai.openai: WARN

